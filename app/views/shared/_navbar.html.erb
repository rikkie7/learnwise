<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand mt-2" do %>
      <%= image_tag "logo_green.png" %>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon" id="navbarToggler"></span>
    </button>
    <div class="collapse navbar-collapse mt-3" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Become a teacher", new_course_path, class: "become-teacher"%>
          </li>
          <li><%= link_to 'Dashboard', dashboard_path, class: 'dashboard' %></li>
          <li class="nav-item me-4">
            <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "logout" %>
          </li>
          <li class="nav-item dropdown">
            <a href="/dashboard" class="avatar navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <% if current_user.photo.attached? %>
                <%= cl_image_tag current_user.photo.key, class: "avatar", alt: "dropdown menu" %>
              <% else %>
                <div class="name-card navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  <%= current_user.first_name[0] + current_user.last_name[0] %>
                </div>
              <% end %>
            </a>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link login" %>
          </li>
        <% end %>
      </ul>
    </div>


  </div>
</div>
<div class="category-bar">
  <ul class="category-list">
    <li class="category-item">
      <%= link_to "Technology", courses_category_index_path("technology"), class: "category-link" %>
    </li>
    <li class="category-item"><%= link_to "Education", courses_category_index_path("education"), class: "category-link" %></li>
    <li class="category-item"><%= link_to "Cooking", courses_category_index_path("cooking"), class: "category-link" %></li>
    <li class="category-item"><%= link_to "Gardening", courses_category_index_path("gardening"), class: "category-link" %></li>
    <li class="category-item"><%= link_to "Sports", courses_category_index_path("sports"), class: "category-link" %></li>
    <li class="category-item"><%= link_to "Others", courses_category_index_path("others"), class: "category-link" %></li>
  </ul>
</div>
<script>
  const toggler = document.getElementById("navbarSupportedContent");
  toggler.addEventListener("hide.bs.collapse", (event) => {
    event.stopImmediatePropagation();
    console.log("test")
  })
  toggler.addEventListener("show.bs.collapse", (event) => {
    event.stopImmediatePropagation();
    console.log("test")
  })
</script>
